if [[ -f ~/.nvm/nvm.sh ]]; then
  source ~/.nvm/nvm.sh
  export NODE_PATH=$NODE_PATH:/home/david/.npm/lib/node_modules
else
  export NVM_DIR=~/.nvm

  if [[ $(which brew) ]]; then
    nvm_script=$(brew --prefix nvm)/nvm.sh

    if [[ -f $nvm_script ]]; then
      source $nvm_script
    fi
  fi
fi
