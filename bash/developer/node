if [[ -f ~/.nvm/nvm.sh ]]; then
  source ~/.nvm/nvm.sh
else
  export NVM_DIR=~/.nvm

  if [[ $(which brew) ]]; then
    nvm_script=$(brew --prefix nvm)/nvm.sh

    if [[ -f $nvm_script ]]; then
      source $nvm_script
    fi
  fi
fi
