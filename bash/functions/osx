# Make font smoothing default on non-Apple external displays
smoothfonts() {
  defaults -currentHost write -globalDomain AppleFontSmoothing -int 2
}

dockify() {
  defaults write com.apple.dock no-glass -bool true
}
